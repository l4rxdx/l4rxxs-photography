# Release Log / 正式上线日志

本文件只记录已经正式上线的网站版本。语言面向发布说明：专业、清晰、精炼，并以“相对上一个正式上线版本的变化”为基准。

## Versioning / 版本规则

- 正式上线版本使用 `vMAJOR.MINOR.PATCH`。
- `MAJOR`：整体结构、导航方式或视觉体系发生大规模重构。
- `MINOR`：新增主要功能、重要交互、内容规模升级或正式上线体验明显变化。
- `PATCH`：线上修复、文案调整、部署配置修正或低风险细节优化。
- 每个正式上线版本必须记录版本号、上线日期、对应提交、上线分支和缓存标识。

## v1.5.1 - Index Stability And Work Withdrawal / 索引稳定与 WORK 暂时撤下版

- 上线日期：2026-07-25
- 对比基准：`v1.5.0 - Motion, Loading And Work Foundation`
- Git commit：本次发布提交，推送后以 GitHub `main` 历史和 Cloudflare Pages 部署记录为准。
- 上线分支：`main`
- 缓存标识：`v151-index-stability1`
- 发布策略：修复菜单返回主页与 INDEX 快速跨照片切换的状态交接；暂时撤下尚未定稿的 WORK 页面及素材，不改变图库内容，图库保持 49 张照片。

### 优化

- INDEX 增加 `260ms` 响应间隔，限制刚展开时立即切图及退出后立即重开，降低连续点击造成的状态竞争。

### 修复

- 修复从菜单点击“总览”返回主页时，页面过渡遮罩未退出造成的白屏。
- 修复 INDEX 完全展开后切换照片、紧接着重开并再次选图时，旧照片卡片的图片节点未归还导致图库缺图。
- 修复不同长宽比照片快速切换时，移动图片沿路径被非等比拉伸或尺寸突变；移动阶段现在使用目标照片的固定比例。

### 删除

- WORK 尚未定稿，暂时从全站菜单移除并清空页面内容；Cloudflare 构建不再发布其媒体目录。

### 部署验证

- `390×844` 与 `1365×900` 视口覆盖 INDEX 展开途中切图、展开完成后切图及 A→快速重开→B 连切路径；49 张卡片完整，无隐藏残留、空节点或可见空白。
- 逐帧采样确认横幅目标照片的移动比例稳定在约 `2.353`，立即重开和立即切图均被 `260ms` 响应间隔拦截。
- 菜单从 rel、WORK 和日志页返回主页均正常退场遮罩，主页照片节点完整，控制台无错误。
- JavaScript 语法检查、`tests/site-check.ps1`、Cloudflare 静态构建、`git diff --check` 和发布前敏感信息复核通过。

## v1.5.0 - Motion, Loading And Work Foundation / 动效、加载与 WORK 基础版

- 上线日期：2026-07-25
- 对比基准：`v1.4.7 - Rail Return Recovery`
- Git commit：本次发布提交，推送后以 GitHub `main` 历史和 Cloudflare Pages 部署记录为准。
- 上线分支：`main`
- 缓存标识：`v150-motion-work1`
- 发布策略：统一主页、rel、INDEX 与菜单之间的视觉交接，保证首屏照片完整加载后再开始动画，并上线 WORK 的首个拼贴页面；图库照片数量保持 49 张。

### 优化

- 主页首屏照片全部解码并预绘后再从中央散开；主页、rel 主图、缩略图和 INDEX 使用照片取色的纯色加载占位，避免空白闪现。
- 主页与 rel 之间改用统一页面遮罩过渡，取消照片飞回主页；菜单使用明确且可打断的进入、打开和退出状态。
- INDEX 选中另一张照片返回 rel 或随记前，会先加载并解码目标清晰图；目标布局使用最终随记几何测量，避免落位后再次换图或偏移。
- 主页头像使用头部外圈椭圆参与文字和照片碰撞，尺寸可穿过照片间隙；落地后保留碰撞产生的自然倾斜，不再自动回正。

### 修复

- 修复主页开场动画中部分照片沿散开路径移动时才由空白变为照片，以及从 rel 返回后主页照片重新显示加载占位的问题。
- 修复从一张照片的随记进入 INDEX，再选择另一张照片返回随记时，末段清晰图交接和最终视觉位置跳动。
- 修复 rel、随记与 INDEX 在快速切换、视口变化和返回时可能复用旧布局或旧图片状态的问题。

### 增加

- WORK 页面新增 2 张图片，并建立首屏拼贴版式。

### 部署验证

- 移动端与桌面端逐帧检查“照片 1 随记 → INDEX → 照片 2 随记”：移动开始后至落位只使用照片 2 的同一清晰资源，未发生末段换图；可见内容落位误差低于 `0.1px`。
- 主页首屏等待完整图片集合解码后再统一揭示；头像物理实测会随照片和边缘碰撞产生自然倾斜，不再被持续拉回 `0deg`。
- JavaScript 语法检查、`tests/site-check.ps1`、Cloudflare 静态构建和 `git diff --check` 通过。
- 发布前未跟踪任何 `.env` 文件；本地完整扫描因图片二进制数量超时，改用发布文本候选敏感模式扫描并人工复核公开邮箱与新增媒体；当前环境未安装 `ggshield`。

## v1.4.7 - Rail Return Recovery / 缩略图轨道返回修复版

- 上线日期：2026-07-23
- 对比基准：`v1.4.6 - Rel Motion And Index Handoff`
- Git commit：本次发布提交，推送后以 GitHub `main` 历史和 Cloudflare Pages 部署记录为准。
- 上线分支：`main`
- 缓存标识：`v147-rail-return1`
- 发布策略：修复上一版本中 INDEX 返回后的移动端缩略图轨道状态，并删去 INDEX 底部多余视觉层；不改变图库内容、动画路径和页面入口。

### 修复

- 修复移动端从 INDEX 选择照片返回后，rel 与随记页面底部缩略图轨道无法继续滑动选图；返回时会重建轨道几何并释放动画锁。

### 删除

- 移除 INDEX 页面底部多余的固定模糊层。

### 部署验证

- `390×844` 视口中，rel 进入 INDEX 选择 `rel=40` 返回后保持正确照片，轨道可继续切至 `rel=44`；随记进入 INDEX 选择 `rel=42` 返回后保持随记，轨道可继续切至 `rel=45`。
- INDEX 底部伪元素计算结果为 `content: none`、`backdrop-filter: none`，页面横向溢出为 `0px`。
- JavaScript 语法检查、`tests/site-check.ps1`、Cloudflare 静态构建和 `git diff --check` 通过。
- 发布前未跟踪任何 `.env` 文件，本地敏感模式扫描无高风险项；公开联系邮箱经人工确认；当前环境未安装 `ggshield`。

## v1.4.6 - Rel Motion And Index Handoff / rel 动效与索引交接版

- 上线日期：2026-07-22
- 对比基准：`v1.4.5 - Runtime Performance And Menu Stability`
- Git commit：本次发布提交，推送后以 GitHub `main` 历史和 Cloudflare Pages 部署记录为准。
- 上线分支：`main`
- 缓存标识：`v146-rel-motion1`
- 发布策略：提高移动端 rel 主图与缩略图轨道的连续滑动流畅度，并统一 rel、随记与 INDEX 之间的底部控件和背景交接；不改变图库内容与操作入口。

### 优化

- 移动端 rel 缩略图轨道改用浏览器原生横向惯性；主图拖动、轨道同步与快速滑动选图按帧合并，减少重复解码和样式计算。
- 进入 INDEX 时底部操作栏与缩略图轨道同步退出；随记与 INDEX 往返背景从动画首帧连续过渡。

### 修复

- 修复快速开关或后台标签页恢复时 INDEX 动画可能停滞、底部控件残留的问题，并在 INDEX 状态禁用已退场控件。

### 部署验证

- 移动端轨道不再拦截原生横向 `touchmove`，主图触摸更新合并为每帧一次，快速轨道滚动以短窗口提交最终照片。
- 浏览器逐帧检查确认随记进入 INDEX 时背景在首段透明度约 `0.19` 已开始变化；INDEX 返回随记时背景和随记底色同步插值。
- JavaScript 语法检查、`tests/site-check.ps1`、Cloudflare 静态构建和 `git diff --check` 通过。
- 发布前未跟踪任何 `.env` 文件，本地敏感模式扫描无高风险项；公开联系邮箱经人工确认；当前环境未安装 `ggshield`。

## v1.4.5 - Runtime Performance And Menu Stability / 运行性能与菜单稳定版

- 上线日期：2026-07-21
- 对比基准：`v1.4.4 - Interaction Stability And Theme Ownership`
- Git commit：本次发布提交，推送后以 GitHub `main` 历史和 Cloudflare Pages 部署记录为准。
- 上线分支：`main`
- 缓存标识：`v145-runtime-menu1`
- 发布策略：降低首页、INDEX 和随记的长时间运行开销，同时修复返回清晰度与移动端中文菜单排版；不改变图库内容和主要操作方式。

### 优化

- 首页图库复用固定批次，文字与装饰小人停稳后完全休眠；滚动、旋转、尺寸变化或页面恢复时再唤醒。
- INDEX 改用原生纵向滚动、可见图片加载和卡片几何缓存；随记布局测量与 INDEX 预热合并调度。
- 随记正文与地点改用系统苹方字体，地点保留粗体层级。

### 修复

- 修复从随记进入 INDEX 再返回时，照片可能复用低清晰度合成纹理而变模糊。
- 修复移动端中文菜单左下角链接换行，并保证复制邮箱状态与右侧版权、日志入口不重叠。

### 部署验证

- 首页空闲 6 秒动画帧请求由约 `1400` 次降为 `0`；桌面、`390×844` 和 `320×568` 稳定休眠，滚动后可唤醒并再次归零。
- 首页连续滚底后节点稳定在 `205` 个；移动端 INDEX 方向滚动横向偏移为 `0px`，49 张缩略图全部加载，连续 8 次快速开关无残留图层。
- 随记与 INDEX 往返保持同一张清晰主图；移动端中文菜单在 `320×568` 与 `390×844` 下单行显示，横向溢出为 `0px`。
- JavaScript 语法检查、`tests/site-check.ps1`、Cloudflare 静态构建和 `git diff --check` 通过。
- 发布前本地敏感信息扫描无高风险项；公开联系邮箱经人工确认；当前环境未安装 `ggshield`。

## v1.4.4 - Interaction Stability And Theme Ownership / 交互稳定与主题所有权版

- 上线日期：2026-07-21
- 对比基准：`v1.4.3 - Gallery Additions And Notes Motion`
- Git commit：本次发布提交，推送后以 GitHub `main` 历史和 Cloudflare Pages 部署记录为准。
- 上线分支：`main`
- 缓存标识：`v144-stability-theme1`
- 发布策略：稳定移动端首页与 INDEX、统一语言和主题偏好的长期状态，并补足随记照片的视觉层次；不改变图库内容与页面结构。

### 修复

- 修复移动端首页照片偶发跳动，并让用户明确选择的语言在刷新、新标签页和后续访问中保持。
- 修复退出 INDEX 后立即打开随记时主图过渡丢失，快速连续操作仍从当前画面平滑接管。
- 修复系统主题变化后站内主题按钮首次点击可能无效；自动模式继续跟随系统，手动主题可跨页面与标签页同步。

### 增加

- 随记照片新增主题自适应轮廓阴影，并沿用随记进入与退出曲线平滑变化。

### 部署验证

- 移动端首页照片在触摸滚动、文字运动和慢网图片解码期间保持 `0px` 位移；语言切换、刷新、新标签页和旧会话迁移均通过。
- 桌面与移动端完成 INDEX 返回后立即开关随记的快速打断验证，无丢失过渡、残留状态或横向溢出。
- 系统主题跟随、手动主题锁定、跨页面同步与连续 21 次快速切换均正确；随记阴影连续 10 次开关的采样帧无超过 `34ms`。
- JavaScript 语法检查、`tests/site-check.ps1`、Cloudflare 静态构建和 `git diff --check` 通过。
- 发布前本地敏感信息扫描无高风险项；公开联系邮箱经人工确认，`.env` 忽略规则正确；当前环境未安装 `ggshield`。

## v1.4.3 - Gallery Additions And Notes Motion / 图库新增与随记曲线版

- 上线日期：2026-07-18
- 对比基准：`v1.4.2 - Loading And Motion Performance`
- Git commit：本次发布提交，推送后以 GitHub `main` 历史和 Cloudflare Pages 部署记录为准。
- 上线分支：`main`
- 缓存标识：`v143-gallery-notes1`
- 发布策略：新增 2 张照片并恢复随记的上一版柔和曲线；保留当前加载策略、同步取色、动画时长和可打断状态管理。

### 优化

- 随记主图、背景、分隔线与文字的进入和退出统一使用 `ease-soft` 曲线 `cubic-bezier(.5, 0, .5, 1)`，保留当前同步启动和中途打断能力。

### 增加

- 新增 2 张照片，图库增至 49 张。

### 部署验证

- 移动端连续快速切换 11 次后主图稳定显示；INDEX 共 49 张卡片，纵向滚动横向漂移为 `0px`。
- 随记执行“打开 → 关闭 → 立即再打开”后稳定收束；浏览器计算曲线为 `cubic-bezier(0.5, 0, 0.5, 1)`，无闪白、丢图或残留状态。
- JavaScript 语法检查、`tests/site-check.ps1`、Cloudflare 静态构建和 `git diff --check` 通过。
- 发布前敏感信息扫描无高风险项；公开联系邮箱经人工确认；当前环境未安装 `ggshield`。

## v1.4.2 - Loading And Motion Performance / 加载与动效性能版

- 上线日期：2026-07-16
- 对比基准：`v1.4.1 - Home Photo Sizing And English Default`
- Git commit：`4f58378`
- 上线分支：`main`
- 缓存标识：`perf-flow3`
- 发布策略：降低移动端照片首屏传输和后台解码开销，减少首页重力系统的空闲计算，并调整随记字体；不改变图库结构和既有交互规则。

### 优化

- 为 45 张照片新增最大边长 `1280px` 的移动端资源；桌面端继续使用原网页图。
- 隐藏 INDEX 不再后台预热；相邻照片在当前主图完成解码后才于空闲阶段加载，预加载缓存限制为 8 张。
- INDEX 可见缩略图按批次加载，Focus 与 INDEX 节点通过文档片段一次挂载，减少首屏请求和重复布局。
- 首页文字重力改为刷新率同步调度，缓存照片碰撞矩形和 transform 状态，降低静止阶段的计算与样式写入。
- 随记正文与地点使用本地手写字体，地点保留粗体层级。

### 部署验证

- 模拟 3G 与 4 倍 CPU 降速：移动端 LCP 由约 `13.0s` 降至 `4.88s`，图片请求由 `32` 降至 `9`，图片传输由约 `1.9MB` 降至 `354KB`。
- 极端交互验证：连续快速滑动、反向切图、退出随记后立即切图、INDEX 快速开合与纵向滚动均稳定收束，无缺图、横移或残留动画状态。
- 桌面 Focus 继续加载 `images/web`，移动端加载 `images/medium`；Work 首屏无缺图或横向溢出。
- JavaScript 语法检查、`tests/site-check.ps1`、Cloudflare 静态构建和 `git diff --check` 通过。
- 发布前敏感信息扫描无高风险项；公开联系邮箱经人工确认，新中尺寸图片无 GPS 标签；当前环境未安装 `ggshield`。

## v1.4.1 - Home Photo Sizing And English Default / 首页照片尺寸与默认英语版

- 上线日期：2026-07-14
- 对比基准：`v1.4.0 - Mobile Index And Notes Locations`
- Git commit：本次发布提交，推送后以 GitHub `main` 历史和 Cloudflare Pages 部署记录为准。
- 上线分支：`main`
- 缓存标识：`v141-manifest4`
- 发布策略：减少首页非方形照片的多余留白，统一新会话的英语默认状态，并新增 1 张照片；不改变 1:1 照片尺寸和既有页面交互。

### 优化

- 首页照片按宽高比使用固定倍率，宽图与竖图适度放大，边缘单元使用更保守倍率；1:1 照片保持原尺寸。
- 首页随机补位从队列中段取图，降低相同照片在无限滚动批次边界附近连续出现的概率。
- 新浏览会话默认使用英语；用户手动切换后，语言在当前会话内跨页面保持。

### 增加

- 新增 1 张照片，图库增至 45 张。

### 部署验证

- 桌面端与移动端各完成 12 轮随机布局测试，无图片碰撞、横向溢出或 1:1 倍率偏移。
- 移动端深度滚动追加 90 个网格单元后，尺寸分类、边缘倍率与图片间距保持稳定。
- 验证新会话默认英语、会话内中文跨页面保持，以及新标签重新恢复英语。
- JavaScript 语法检查、`tests/site-check.ps1`、Cloudflare 静态构建和 `git diff --check` 通过。
- 发布前敏感信息扫描无高风险项；当前环境未安装 `ggshield`，未覆盖 GitGuardian 自动扫描。

## v1.4.0 - Mobile Index And Notes Locations / 移动索引与随记地点版

- 上线日期：2026-07-14
- 对比基准：`v1.3.0 - Focus Index And Notes Scene`
- Git commit：本次发布提交，推送后以 GitHub `main` 历史和 Cloudflare Pages 部署记录为准。
- 上线分支：`main`
- 缓存标识：`notes-location-release1`
- 发布策略：稳定移动端索引图库的单轴纵向交互和图片就绪状态，并扩充随记的双语地点与正文内容；不改变现有网站视觉样式。

### 优化

- 移动端索引图库改为固定页面上的单轴纵向手势，卡片在图片加载前预留稳定尺寸，并使用更柔和一致的进入与退出曲线。
- 优化短屏随记布局与随记状态切图，使照片完整滑出和滑入屏幕，同时保留快速连续操作的响应。

### 修复

- 修复移动端索引图库纵向滑动时画面横向乱跳的问题。
- 修复索引图库偶发缺图、缩略图加载失败后不重试，以及开合结束后残留位移的问题。

### 增加

- 随记正文上方新增可选双语地点行；无地点的照片不显示空白占位。
- 为 rel `1`、`3`、`5`、`9`、`11`、`12`、`14`、`26`、`29`、`32`、`33`、`39`、`42`、`44` 补充双语地点与随记。

### 部署验证

- 极端手势验证：移动端索引图库连续斜向快速滑动 109 个采样点，横向漂移保持 `0px`。
- 边界布局验证：`390×844` 与 `320×568` 中英文移动视口通过，最长地点和随记不与缩略图轨道重叠。
- 本地验证：JavaScript 语法检查、`tests/site-check.ps1`、Cloudflare 静态构建和 `git diff --check` 通过。
- 发布前检查：本地敏感信息扫描无高风险项；当前环境未安装 `ggshield`，未覆盖 GitGuardian 自动扫描。

## v1.3.0 - Focus Index And Notes Scene / 索引图库与随记场景版

- 上线日期：2026-07-14
- 对比基准：`v1.2.1 - Return Flight Stability`
- Git commit：本次发布提交，推送后以 GitHub `main` 历史和 Cloudflare Pages 部署记录为准。
- 上线分支：`main`
- 缓存标识：`focus-index-release1`
- 发布策略：正式开放 rel `INDEX / 索引` 图库，上线移动端连续滑动、照片取色随记背景与图库加载优化，新增 1 张照片，并删除不符合最终交互方向的桌面大图滑动与首页倾斜透视。

### 优化

- 优化移动端 rel 大图连续滑动与缩略图同步，使快速反向和连续输入从当前画面稳定接管。
- 优化 INDEX 索引图库的开合、滚动和图片加载，使主图与周边图片同步运动，并降低移动端解码与布局开销。
- 优化随记背景，按当前照片取色并随页面开合缓入缓出，同时降低明暗主题底色干扰。
- 优化图库首屏加载和版本化资源缓存，延后非关键缩略图与索引资源。

### 修复

- 修复退出随记后立即切图时大图消失的问题；慢网或连续改选时保留当前图直到目标图解码完成。
- 修复 INDEX 偶发缺图、纵向滚动时页面横移、开合跳帧，以及主图回位时出现额外过渡的问题。
- 修复快速切换页面或动画抢占时旧请求继续落地、主图状态与 rel 编号不同步的问题。
- 修复移动端 rel 滑动掉帧和缩略图轨道动画阻塞下一次输入的问题。

### 删除

- 移除桌面端 rel 大图滑动切换，桌面端继续使用缩略图、点击和滚轮浏览。
- 移除首页照片随设备倾斜产生的透视效果。

### 增加

- 正式加入 rel `INDEX / 索引` 图库，支持移动端纵向浏览、同步开合和可打断动画。
- 新增按当前照片取色的随记背景，并为索引页顶部与底部操作区域加入渐隐模糊。
- 新增 1 张照片，图库增至 44 张。

### 部署验证

- 极端情况验证：慢网等待、80ms 连续改选、退出随记后立即切图、INDEX 抢占、末张边界和 `390×844` 移动视口均通过。
- 本地验证：`tests/site-check.ps1` 通过；`scripts/cloudflare-build.mjs` 成功输出 `dist`。
- 发布前检查：本地敏感信息扫描无高风险项；占位邮箱与图片二进制命中已人工核对。

## v1.2.1 - Return Flight Stability / 返回落位稳定性修复版

- 上线日期：2026-07-12
- 对比基准：`v1.2.0 - Spatial Motion And Continuous Swipe`
- Git commit：本次发布提交，推送后以 GitHub `main` 历史和 Cloudflare Pages 部署记录为准。
- 上线分支：`main`
- 缓存标识：`return-stability-release1`
- 发布策略：只修复 rel 返回首页的高 Z 轴照片落位，不改变首页、随记、主题、语言或 rel 大图滑动规则。

### 优化

- 优化 rel 返回首页的高 Z 轴照片落位，使飞行层从 rel 源图的真实位置和比例连续缩放至首页目标。

### 修复

- 修复透视重复放大导致返回动画首帧尺寸和位置漂移的问题。
- 修复图片解码较慢、动画中滚动或最终图层交接时可能出现的跳位、提前结束和短暂闪动。

### 删除

- 本版本未删除面向访问者的功能。

### 增加

- 增加返回动画图片就绪保护、独立安全计时和短时滚动位置锁定。

### 部署验证

- 首帧几何对比：修复前飞行层由约 `574×410` 被透视放大为约 `1043×745`；修复后位置与宽高误差约 `0.01px`。
- 极端情况验证：桌面端、移动端末张照片、首页边缘目标、动画中强制滚动和最终图层交接均通过。
- 本地验证：`tests/site-check.ps1` 通过；`scripts/cloudflare-build.mjs` 成功输出 `dist`。

## v1.2.0 - Spatial Motion And Continuous Swipe / 空间动效与连续滑动版

- 上线日期：2026-07-12
- 对比基准：`v1.1.3 - Mobile Interaction And Focus Swipe`
- Git commit：本次发布提交，推送后以 GitHub `main` 历史和 Cloudflare Pages 部署记录为准。
- 上线分支：`main`
- 缓存标识：`spatial-interactions-release1`
- 发布策略：上线首页空间重力互动、全站主题过渡和 rel 大图连续滑动；继续暂缓 rel `INDEX / 索引` 图库动画。

### 优化

- 优化首页 `L4RXX` 重力系统：增强移动端横纵倾斜响应，让文字在照片空白区域自然靠拢，并降低静止阶段的抖动与误碰。
- 优化 rel 大图切换：移动端横向、桌面端纵向滑动，并依据相邻照片的真实可见尺寸计算间距、缩放、淡入淡出与纵深变化。
- 优化快速连续滑动：新手势从当前画面位置继续，主图与缩略图轨道分离推进，连续手势不再等待轨道动画结束。
- 统一全站明暗主题的颜色过渡，并以共享渐隐雾化边缘和轻微轮廓阴影提升顶部导航图标的可读性。
- 优化从 rel 返回首页的照片落位，使当前原图沿单段高 Z 轴轨迹回到原位置，并稳定交接给首页照片。

### 修复

- 修复快速连续滑动被缩略图轨道动画阻塞、手势被吞掉，以及反向打断后仍继续播放旧队列的问题。
- 修复 rel 大图取消滑动、首尾边界回弹和图层交接时可能出现的闪亮、残留帧或错误绕回。
- 修复首页开场个别照片乱跳，以及从 rel 返回首页时照片二段跳和落位交接不连续的问题。
- 修复菜单切换主题时文字、地球图标细线与头像变色时点不一致的问题。

### 删除

- 删除顶部三个导航图标各自独立的模糊底板，改用一条共享且渐隐的背景雾化区域。

### 增加

- 新增首页照片层随手机倾斜产生的轻微透视位移，以及 `L4RXX` 再次合体后的呼吸释放彩蛋。
- 新增桌面端 rel 大图纵向鼠标拖动、触控笔和滚轮切换，并保留移动端横向手势。
- 新增 rel 大图连续滑动队列，支持最多三步有边界的快速输入，并兼容普通 rel 与随记状态。

### 部署验证

- 本地验证：`tests/site-check.ps1` 通过。
- 本地构建：`scripts/cloudflare-build.mjs` 成功输出 `dist`。
- 本地浏览器验证：移动端三连滑从 `rel=20` 稳定到 `rel=23`，反向打断只完成当前一步；随记状态双连滑后文字、分隔线和临时图层均正确恢复；桌面端纵向三连滑与反向取消均通过。

## v1.1.3 - Mobile Interaction And Focus Swipe / 移动交互与大图滑动优化版

- 上线日期：2026-07-10
- 对比基准：`v1.1.2 - Release Log Correction`
- Git commit：本次发布提交，推送后以 GitHub `main` 历史和 Cloudflare Pages 部署记录为准。
- 上线分支：`main`
- 缓存标识：`stable-swipe-release1`
- 发布策略：上线已稳定的移动端体验、日志入口、随记稳定性与 rel 大图横向滑动；继续暂缓 rel `INDEX / 索引` 图库动画。

### 优化

- 优化 rel 页面大图横向滑动，支持一张一张切换、缩略图同步、快速连续手势与更柔和的释放动画。
- 优化随记页语言与主题切换时的文案过渡和布局稳定性，减少图片、随记线和文字跳动。
- 优化菜单日志入口，改为更清晰的纸张图标，并让日志内容直接占用菜单页面。
- 优化移动端首页 `L4RXX` 类重力互动，接入设备方向并加入低通滤波。

### 修复

- 修复 rel 大图滑动后缩略图轨道失效、首尾图片绕回、取消滑动闪烁等问题。
- 修复系统主题变化时网站自动主题未跟随的问题。
- 修复随记页面多次切换语言后不自适应的问题。
- 修复日志页切换主题时文字闪烁，以及桌面端菜单加号头像显示异常的问题。

### 删除 / 暂缓

- 移除移动端 rel 页面下滑进入沉浸大图模式的手势，保留随记页面下滑退出。
- rel `INDEX / 索引` 仍保持暂缓状态，本次发布不包含索引图库交互修复。

### 增加

- 新增移动端随记上滑进入、下滑退出手势。
- 新增 rel 页面大图横向滑动切换，支持普通 rel 状态，并兼容随记状态。

### 部署验证

- 本地验证：`tests/site-check.ps1` 通过。
- 本地构建：`scripts/cloudflare-build.mjs` 成功输出 `dist`。
- 本地浏览器验证：普通 rel 大图滑动、快速连续滑动、首图边界回弹均通过，控制台无相关错误。

## v1.1.2 - Release Log Correction / 正式日志校正版

- 上线日期：2026-07-09
- 对比基准：`v1.1.1 - Versioned Log System`
- Git commit：本次发布提交，推送后以 GitHub `main` 历史和 Cloudflare Pages 部署记录为准。
- 上线分支：`main`
- 缓存标识：`logs-history-fix1`
- 发布策略：只修正日志与日志预览入口，不改首页、rel 页面、照片互动、随记或主题系统。

### 增加

- 补充历史正式版本 `v1.0.1 - Gallery Expansion And Image Optimization / 图库扩展与图片优化版`，记录 2026-07-07 的新照片资源批次。
- 为线上日志预览增加 `/logs` clean URL 兼容输出，避免 Cloudflare 将 `/logs.html` 跳转到 `/logs` 后找不到页面。

### 优化

- 调整 `v1.1.0` 的对比基准为 `v1.0.1`，让“照片资源批次”和“非 INDEX 稳定体验”分开记录。
- 将照片批次的数量写成可验证口径：公开图库从 25 张 rel 照片扩展到 43 张，新增 18 个公开 rel 条目，并重新压缩生成 web 图和缩略图。

### 修复

- 修复正式日志遗漏新照片资源正式版本的问题。
- 修复线上日志预览页在 Cloudflare clean URL 规则下可能无法通过 `/logs` 打开的问题。

### 删除 / 暂缓

- 未删除现有网站功能。
- rel `INDEX / 索引` 仍保持暂缓状态，本次发布不包含索引图库交互修复。

### 部署验证

- 本地验证：`tests/site-check.ps1` 通过。
- 本地构建：`scripts/cloudflare-build.mjs` 成功输出 `dist`，并包含 `logs.html` 与 `logs/index.html`。

## v1.1.1 - Versioned Log System / 版本化日志系统

- 上线日期：2026-07-09
- 对比基准：`v1.1.0 - Non-INDEX Portfolio Release`
- Git commit：`d49ae85`
- 上线分支：`main`
- 缓存标识：`logs-html1`
- 发布策略：只上线日志体系、版本规范与浏览器日志预览页；继续保持 rel `INDEX / 索引` 入口暂缓，不把未稳定的索引动画带回线上。

### 增加

- 新增 `logs.html` 日志预览页，提供 `Index / Release / Work` 三个标签入口，用浏览器页面统一查看更新日志、正式发布日志和工作日志。
- 新增 UTF-8 Markdown 读取逻辑，预览页通过 `TextDecoder("utf-8")` 解析日志源文件，避免本地静态服务器没有 `charset=utf-8` 时出现中文乱码。
- 新增对 `logs.html` 的构建输出与站点检查覆盖，确保 Cloudflare Pages 构建产物包含日志预览页。

### 优化

- 将原 `UPDATE_LOG.md` 调整为日志总入口，只保留当前版本、上线信息、日志分类和版本规则。
- 将正式发布记录与实际工作记录分离：`RELEASE_LOG.md` 面向正式版本说明，`WORK_LOG.md` 面向开发、排查、试错和未上线内容追踪。
- 将项目版本从 `1.1.0` 同步提升到 `1.1.1`，使本次日志系统上线拥有独立版本号。

### 修复

- 修复本地直接打开 `.md` 日志时可能出现中文乱码的问题；推荐通过 `logs.html` 阅读日志，Markdown 文件继续作为版本记录源文件保留。

### 删除 / 暂缓

- 未删除现有网站功能。
- rel `INDEX / 索引` 仍保持暂缓状态，本次发布不包含索引图库交互修复。

### 部署验证

- 本地验证：`tests/site-check.ps1` 通过。
- 本地构建：`scripts/cloudflare-build.mjs` 成功输出 `dist`，并包含 `logs.html`、`UPDATE_LOG.md`、`RELEASE_LOG.md` 与 `WORK_LOG.md`。
- 本地预览：`http://127.0.0.1:8765/logs.html#work` 中文正常显示，无乱码。

## v1.1.0 - Non-INDEX Portfolio Release / 非索引作品集版

- 上线日期：2026-07-09
- 对比基准：`v1.0.1 - Gallery Expansion And Image Optimization`
- Git commit：`dce2a7b`
- 上线分支：`main`
- 缓存标识：`no-index-release1`
- 发布策略：保留已稳定的首页、rel 页面、随记、主题与语言系统；暂时移除不稳定的 rel `INDEX / 索引` 入口，待单独修复后再作为后续版本上线。

### 增加

- 新增白天 / 黑夜主题系统，支持系统主题自动识别，并加入与顶部符号按钮统一的主题切换控件。
- 新增 rel 页面随记模式：点击大图可展开 / 收回随记，随记内容支持中文与英文切换，并保留未来逐张照片补充文字的结构。
- 新增 favicon、manifest、robots、sitemap、404、README 与 Cloudflare Pages 静态构建入口，补齐正式部署所需基础文件。

### 优化

- 优化首页照片顺序：普通进入首页时随机排列照片，从 rel 页面 BACK 返回时保持原点击位置与当时的随机顺序。
- 优化首页 `L4RXX` 重力互动：字母与照片产生碰撞反馈，并加入移动端设备方向影响与人物彩蛋，使首页更有继续滑动浏览的动势。
- 优化中英文切换与主题按钮的视觉系统：语言按钮使用经纬线地球符号，主题按钮使用太阳 / 月亮符号，并加入头像线条悬浮细节。
- 优化 rel 页面缩略图轨道：支持更柔和的滑动、视差感和点击后的轨道跟随，减少直接跳动感。
- 优化随记页面布局：大图尺寸、随记线位置与文字宽度会根据照片比例、视口尺寸和文案长度自适应，兼顾桌面端与移动端比例。
- 优化导航菜单背景模糊：桌面端和移动端均使用高斯模糊，使菜单内容保持清晰，同时保留背景照片的氛围。

### 修复

- 修复移动端首页点击照片进入错误 rel 页的问题，保持每张照片与对应 rel 编号一致。
- 修复从 rel 页面 BACK 返回首页时回到第一页并重播动画的问题，现在会回到刚刚点击照片的位置。
- 修复普通首页刷新、返回状态、随机顺序与移动端点击时序造成的页面混乱问题。
- 修复 rel 页面随记内容在语言切换后没有同步切换的问题。
- 修复加号菜单打开后底层 BACK、INDEX、INFO 等控件仍可被点击的问题。
- 修复 Cloudflare Pages 执行 `npm run build` 时缺少 `package.json` 导致构建失败的问题；现在会构建到 `dist`。

### 删除 / 暂缓

- 删除 rel 页面旧的 `INFO` 操作入口。
- 暂时移除 rel 页面 `INDEX / 索引` 入口；相关动画代码保留在工作分支语境中继续修复，但不进入本次正式上线体验。

### 部署验证

- 本地验证：`tests/site-check.ps1` 通过。
- 本地构建：`scripts/cloudflare-build.mjs` 成功输出 `dist`。
- 线上验证：首页与 rel 页面均加载 `no-index-release1`，rel 页面不再包含 `data-focus-index`。

## v1.0.1 - Gallery Expansion And Image Optimization / 图库扩展与图片优化版

- 上线日期：2026-07-07
- 对比基准：`v1.0.0 - Initial Cloudflare Release`
- Git commit：`c53acb8`
- 上线分支：`main`
- 缓存标识：早期照片资源批次，无独立 query cache key。
- 发布策略：将新照片批次正式加入公开图库，并统一压缩公开 web 图与缩略图，改善加载速度。

### 增加

- 新增 18 张照片，公开图库由 25 张扩展至 43 张。

### 优化

- 重新生成全部公开 web 图，最长边控制在 1920px，JPEG 质量使用 82。
- 重新生成全部缩略图，最长边控制在 480px，JPEG 质量使用 74。
- 更新 `images/og-image.jpg`，保持分享预览图与当时公开图库一致。

### 修复

- 修复图库数据与新增照片资源之间的同步问题，确保新增照片在首页与 rel 页面都能通过稳定 rel 编号访问。

### 删除 / 暂缓

- 从公开图库移除 1 张不再展示的照片，最终公开图库保持 43 张。

### 部署验证

- 本地图库检查确认 `content/photos.json` 包含 43 个条目，且新增照片资源存在于 `images/web` 与 `images/thumbs`。
- 旧更新日志记录该版本已在本地 review 后推送到 GitHub 和 Cloudflare Pages。

## v1.0.0 - Initial Cloudflare Release / 初始正式上线版

- 上线日期：2026-07-07
- Git commit：`b7fcb7f`
- 上线分支：`main`
- 发布范围：建立 l4rxx 摄影作品集的 Cloudflare Pages 静态站点基础版本，包含首页图库、rel 详情页、压缩图片资源、基础构建与更新日志输出。

### 发布内容

- 建立静态摄影作品集结构：`index.html`、`focus.html`、`work.html`、`assets`、`content/photos.json`、`images/web`、`images/thumbs`。
- 建立 Cloudflare Pages 基础部署流程，并将更新日志纳入构建输出。
- 建立照片生成脚本与基础站点检查脚本，为后续持续添加照片和上线更新提供基础。
